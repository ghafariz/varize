import { diff } from './diff';
import { merge } from './merge';
import { obj } from './obj';
import { pick } from './pick';

export { diff, obj, merge, pick };

const varize = { diff, obj, merge, pick };

export default varize;
